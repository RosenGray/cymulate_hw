import StyledGlobalLocation, { GLPlaceholder, Title } from './GlobalLocation.styles';


const GlobalLocation = () => {
    return <StyledGlobalLocation>
        <Title>Global Locations</Title>
        <GLPlaceholder/>
    </StyledGlobalLocation>
}

export default GlobalLocation;